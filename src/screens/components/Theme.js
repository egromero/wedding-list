import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    neutral: {
      main: "#fcb7af",
      contrastText: "#fff",
    },
    complement: {
      main: "#b0f2c2",
      contrastText: "#fff",
    },
  },
});

export default theme;
